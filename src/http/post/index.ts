// import { z } from 'zod'
import { uploads } from "./uploads"

// enum ValidMethods {
//   POST = 'POST',
// }

// const route = z.object({
//   route: z.string().min(1).startsWith('/'),
//   method: z.nativeEnum(ValidMethods),
//   handler: z.function(),
// })

export const handlersPost = [
  // route.parse(
    uploads
// )
]